version: '3.8'

services:
  whoop-scraper:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/downloads
    environment:
      - RUST_LOG=info
      - CHROME_PATH=/usr/bin/chromium
      - DOWNLOAD_DIR=/downloads
    restart: unless-stopped 